services:
  lightpanda:
    image: lightpanda/browser:nightly
    command: serve --host 0.0.0.0 --port 9222
    expose:
      - "9222"
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - CDP_URL=ws://lightpanda:9222
      - PORT=3000
    depends_on:
      - lightpanda
